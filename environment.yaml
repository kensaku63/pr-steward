name: default
description: "PR Steward Agent runtime environment"
config:
  packages:
    apt: [gh, jq]
  networking:
    type: unrestricted
  env:
    - name: GH_TOKEN
      purpose: GitHub API access for gh CLI (PR view / checkout / comment / push)
